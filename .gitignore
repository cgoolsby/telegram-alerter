# Built binary
/telegram-alerter

# --- credentials: never commit these ---
# Real k8s secret manifests (only secret.example.yaml belongs in git)
k8s/secret.yaml
k8s/*secret*.yaml
!k8s/secret.example.yaml
# Env files with tokens (the placeholder example is allowed)
.env
.env.*
*.env
!.env.example
# Generic credential/key files
credentials*
*.pem
*.key
token.txt
